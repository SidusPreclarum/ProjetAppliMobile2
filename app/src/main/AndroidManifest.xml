<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ensai.projetapplimobile">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-library android:name="com.google.android.maps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DateActivity">
        </activity>

        <activity android:name=".CleActivity">
        </activity>

        <activity android:name=".ResultatCle">
        </activity>

        <activity android:name=".ViewSpectacle">
        </activity>

        <activity android:name=".RechercheActivityImp">
        </activity>
        <activity android:name=".ResultatRecherche">
        </activity>
        <activity android:name=".VoirSpectacle">
        </activity>

    </application>

</manifest>